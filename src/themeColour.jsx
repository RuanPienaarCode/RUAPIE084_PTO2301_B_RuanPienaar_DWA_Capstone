import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#38ecec',
    },
    secondary: {
      main: '#FFF',
    },
    tertiary: {
      main: '#ff0054',
    },
    quaternary: {
      main: '#ffbd00',
    },
  },
});

export default theme;
